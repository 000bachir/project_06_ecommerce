// if i found any source of it adding user and session types 

// this exemple is provided by gemini 
/**
export interface Session {
    // Define your session properties here
    expires_at: number;
    access_token: string;
    refresh_token: string;
    // ... other session properties
}

export interface User {
    id: string;
    email: string;
    // ... other user properties
}
 */

